server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location = / {
    return 302 $scheme://$http_host/login;
  }
  
  location / {
    try_files $uri $uri/ /index.html;
  }
}
